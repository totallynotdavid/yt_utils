{
  "name": "yt_utils",
  "private": true,
  "workspaces": ["packages/*/*", "apps/*", "tools/*"],
  "scripts": {
    "lint": "oxlint packages apps tools",
    "fmt": "oxfmt packages apps tools",
    "fmt:check": "oxfmt --check packages apps tools",
    "typecheck": "bun run --workspaces typecheck",
    "test": "vitest run --passWithNoTests",
    "fallow": "fallow",
    "fallow:check": "fallow audit",
    "check": "bun run typecheck && bun run lint && bun run fmt:check && bun run test && bun run fallow:check"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "bunup": "^0.16.32",
    "fallow": "^2.96.0",
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "packageManager": "bun@1.3.14"
}
