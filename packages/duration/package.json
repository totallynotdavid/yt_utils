{
  "name": "@ytutils/duration",
  "version": "0.1.0",
  "private": true,
  "description": "Fetch and format YouTube video duration",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests --config ../../vitest.config.ts"
  },
  "dependencies": {
    "@ytutils/core": "workspace:*",
    "@ytutils/metadata": "workspace:*"
  }
}
