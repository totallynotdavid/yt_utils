{
  "name": "@ytutils/metadata",
  "version": "0.1.0",
  "private": true,
  "description": "Fetch YouTube content metadata from URLs or search queries",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@ytutils/core": "workspace:*",
    "dotenv": "^16.4.5"
  }
}
