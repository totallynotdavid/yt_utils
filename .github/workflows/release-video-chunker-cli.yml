name: release-video-chunker-cli

on:
  push:
    tags:
      - 'video-chunker-cli-v*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/publish-package.yml
    with:
      package_dir: apps/video-chunker-cli
      package_name: '@ytutils/video-chunker-cli'
      tag_prefix: 'video-chunker-cli-v'
