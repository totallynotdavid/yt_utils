name: release-core

on:
  push:
    tags:
      - 'core-v*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/publish-package.yml
    with:
      package_dir: packages/core
      package_name: '@ytutils/core'
      tag_prefix: 'core-v'
