name: release-video-processor

on:
  push:
    tags:
      - 'video-processor-v*'

jobs:
  publish:
    uses: ./.github/workflows/publish-package.yml
    with:
      package_dir: packages/video-processor
      package_name: '@ytutils/video-processor'
      tag_prefix: 'video-processor-v'
